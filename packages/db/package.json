{
  "name": "@facin/db",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed-compras": "npx ts-node --transpile-only seed-compras.ts",
    "db:seed-vendas": "npx ts-node --transpile-only seed-vendas.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0"
  },
  "devDependencies": {
    "@facin/config": "workspace:*",
    "prisma": "^5.15.0",
    "typescript": "^5.4.0"
  }
}
